class Actividad {
  constructor(id, titulo, descripcion, fecha, usuario_id) {
    this.id = id;
    this.titulo = titulo;
    this.descripcion = descripcion;
    this.fecha = fecha;
    this.usuario_id = usuario_id;
  }
}

module.exports = Actividad;
